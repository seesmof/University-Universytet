#include <bits/stdc++.h>
using namespace std;

int main()
{
    cout << endl
         << "/////////////////////////////////////////////////////////////" << endl
         << endl;

    ifstream in1("data.bin", ios::binary);
    ifstream in2("dataa.bin", ios::binary);
    ofstream out1("output.bin", ios::binary);

    while (!in1.eof() && !in2.eof())
    {
        int i = 0;
    }

    cout << endl
         << "/////////////////////////////////////////////////////////////" << endl
         << endl;
    return 0;
}