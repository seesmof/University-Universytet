// include necessary libraries
#include <iostream>
using namespace std;

// declare main function
int main(int argc, char **argv)
{
    // declare variables A and B
    int iA, iB;

    // ask the user to input A and B
    cout << "Please enter A and B: ";
    cin >> iA >> iB;

    // create for loop to devide the numbers by two
    // create a new variable to keep track of the number of iterations the loop had
    // if the iterations number is more than 5, output an error, otherwise output success message
}