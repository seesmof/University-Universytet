Який стандарт MPI та яку назву має біблиотека (реалізація) функцій MPI, які використовуються в даній лабораторній роботі?
Які функції вихідної програми завдання 1 виконуються у всіх процесах?
Що означає ідентифікатор MPI_COMM_WORLD?
Для чого використовується власний номер процесу в комунікаторі?
Напишіть фрагмент паралельної програми, який використовує значення кількості процесів в області зв’язку.
Напишіть фрагмент паралельної програми, в кожному з процесів якої створюється масив динамічної пам’яті, розмір якого дорівнює добутку номера процесу на загальну кількість процесів.
До якого класу належать програми даної лабораторної роботи: SIMD чи MIMD?
