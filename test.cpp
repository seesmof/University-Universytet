#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    double x = 3.99;
    double y = 4;
    double f;

    // f = max(x, y);
    // f = min(x, y);
    // f = pow(2, 3);
    // f = sqrt(9);
    // f = abs(-345);
    // f = round(x);
    // f = ceil(x);
    // f = floor(x);

    cout << f << endl;
}