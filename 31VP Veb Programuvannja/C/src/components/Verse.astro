---
const { text, reference } = Astro.props;

const Books_info_uri = "https://bolls.life/get-books/HOM/";
const Books_info = await fetch(Books_info_uri).then((res) => res.json());

const get_Book_number = (Book_name) => {};

const BIBLE_base_uri = "https://bolls.life/HOM/";
let Book,
  Chapter_Verse = reference.split(" ");
let Book_number = get_Book_number(Book);
---

<blockquote
  class="border-l-4 border-lime-700 flex flex-col gap-1 rounded-md bg-lime-100/50 overflow-hidden"
>
  <cite class="bg-lime-100 py-1 px-3 not-italic capitalize">♰ {reference}</cite>
  <p class="font-bold py-2 px-3">{text}</p>
  <p>{Books_info}</p>
</blockquote>

<script>
  const Books_info_uri = "https://bolls.life/get-books/HOM/";
  const Books_info = await fetch(Books_info_uri).then((res) => res.json());
  console.log(Books_info);
</script>
